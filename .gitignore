# Build output
bin/
obj/
*.user

# Local install / run artefacts
*.log
harbor.log*

# Configuration is never committed. It holds machine-specific paths, internal
# hostnames and port layouts, and belongs to the machine, not the repo.
seed/
servers.json
servers*.json
!src/**/*.json

# Installer build output
build/installer/Output/
